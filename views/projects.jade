extends layout

block sidebar-left
	div(class='sidebar-left')

block content
	h1=title
	if projects
		each project in projects
			include ./projectrow.jade
	else 
		p There is no project registered right now.

block sidebar-right
	div(class='sidebar-right')